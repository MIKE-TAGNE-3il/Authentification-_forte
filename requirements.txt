# ── Authentification Forte — dépendances Python ──────────────────────────────
# Généré pour Python 3.13 (compatible 3.11+)
# Usage : pip install -r requirements.txt

# Web framework
Flask==3.1.2
Werkzeug==3.1.5

# Base de données
PyMySQL==1.1.2

# Sécurité / hachage
bcrypt==5.0.0
Flask-Bcrypt==1.0.1
cryptography>=43.0.0

# Variables d'environnement
python-dotenv==1.2.1

# WebAuthn (empreinte digitale, clé FIDO2)
webauthn==2.7.1

# FIDO2 (carte Cryptonox)
fido2==2.1.1
cbor2==5.9.0
pyOpenSSL==26.0.0
pyasn1==0.6.3
oscrypto==1.3.0
certvalidator==0.11.1
cffi>=1.0.0

# NFC / PC-SC (lecteur carte sans contact)
pyscard==2.3.1

# HTTP client (utilisé par certains modules)
requests==2.33.0

# Serveur WSGI de production
gunicorn==23.0.0
